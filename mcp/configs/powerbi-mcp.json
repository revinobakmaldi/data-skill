{
  "_comment": "Microsoft Power BI Modeling MCP — DAX generation and semantic model edits",
  "_docs": "https://github.com/microsoft/powerbi-modeling-mcp",
  "_capabilities": [
    "Generate and explain DAX measures",
    "Inspect and edit semantic model tables, columns, and relationships",
    "Auto-document measures and columns",
    "Enforce naming conventions across semantic model",
    "Validate DAX syntax before applying to the model"
  ],

  "_setup_claude_code": [
    "Run once to register with Claude Code:",
    "claude mcp add powerbi-modeling-mcp -- npx -y @microsoft/powerbi-modeling-mcp@latest --start",
    "",
    "Flags:",
    "  --readonly          Read-only mode (no model changes)",
    "  --skipconfirmation  Skip per-operation confirm prompts",
    "  --authmode interactive|serviceprincipal"
  ],

  "_setup_remote": [
    "Alternative: remote cloud endpoint (requires Power BI admin to enable tenant setting)",
    "URL: https://api.fabric.microsoft.com/v1/mcp/powerbi",
    "Requires Build permissions on at least one Power BI semantic model"
  ],

  "powerbi-modeling-mcp": {
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "@microsoft/powerbi-modeling-mcp@latest",
      "--start"
    ]
  }
}
