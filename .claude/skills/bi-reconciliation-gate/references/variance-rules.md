# Variance rules

Use simple severity buckets:

- `pass`: no unexplained variance on critical KPIs
- `warning`: non-critical variance or explainable variance pending business confirmation
- `fail`: unexplained variance on critical KPIs

Always name:

- the KPI
- the comparison grain
- the source used
- whether the variance is expected, explainable, or unknown
