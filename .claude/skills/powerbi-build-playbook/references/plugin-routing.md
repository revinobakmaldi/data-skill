# Plugin routing for report build work

- `reports` is the default for report-page and visual work.
- `pbip` is required when editing PBIP/PBIR/TMDL project files directly.
- `pbi-desktop` is useful when the user wants live Desktop interaction or query capture.
- Report-page work should not be described as semantic-model work unless model changes are actually required.
