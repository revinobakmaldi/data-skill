# v0.24.5

- Fixing issue with tmdl-validator binary
- Note that tmdl-validator is an unsigned binary which validates tmdl files. It's possible it's recognized as a false positive by AVs. It's a temporary solution until the release of the TE3 CLI.
