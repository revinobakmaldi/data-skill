# v0.9.3

- Fixed structural issues across the theme skill — visual-type override files were missing the visualStyles/"*" wrapper and using object literals instead of array format
- Corrected theme-compliance jq paths and input_filename piping, textClasses bare hex format, and removed a fabricated title.background property
- Fixed promoting-formatting property mapping errors: matrix theme key (matrix to pivotTable), labels to dataLabels for line/bar/area, fontSize to textSize for slicer, and font short name instead of CSS font stack
- Trimmed the theme skill README entry
