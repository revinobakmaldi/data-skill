# 0.6.0

- Restructured into multi-plugin marketplace with scoped skill discovery
- Each plugin (tabular-editor, pbi-desktop, pbip) now has its own source directory
- Fixed DAX column qualification (all references now fully qualified)
- Added shell escaping, execution policy, and ADOMD.NET column index guidance
