# v0.9.1

- Fixed a batch of inaccuracies in the pbir-format skill — KPI query roles, scatter chart axis roles, boolean literal syntax, datetime precision (7 decimal places), and conditional formatting color token corrections including a reversal that incorrectly excluded CSS color names
- Expanded the pbir-format task lookup table with missing entries, added the new card visual to query role docs, and updated report-rebinding to document pbir CLI as the primary approach for rebind and thick/thin conversion
- Added the theme skill to the README
