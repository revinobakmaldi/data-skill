# v0.18.1

- Fix M comment syntax in power-query skill
- Fix TE2 CLI skill: correct deployment flags, exit codes, save flags, scope to TE2 only
- Fix DAX anti-patterns accuracy (DIVIDE nuance, iterator callbacks, KEEPFILTERS)
- Fix Power Query folding claims (TransformColumnTypes caveat, Table.LastN, Table.StopFolding)
- Fix execute_dax.py silent error handling on DAX failures
- Fix get_model_info.py non-existent upstreamReports endpoint
- Strip te CLI references from all plugins (not yet publicly released)
- Fix fab desc all (corrected to fab desc)
- Fix visual count inconsistency in review-report
- Fix step numbering in review-semantic-model
- Consolidate duplicate IsAvailableInMdx entries
