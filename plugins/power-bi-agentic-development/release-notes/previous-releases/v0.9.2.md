# v0.9.2

- Fixed another batch of inaccuracies in the pbir-format skill — ComparisonKind enum swap, linearGradient3 bounds, SourceRef Source vs Entity gotcha, scopeId entity references, and "page" vs valid visualType clarification
- Expanded selector docs with id selector values (default, selected, hover/press), visual calculation window functions to all 15 with dax.guide links, and compound axis values
- Corrected report-extensions visual calculation placeholder structure and documented required root fields ($schema, name, entities)
- Expanded inferring-queries with pivotTable (matrix) roles, tableEx DAX examples, and a broader roles table covering all common chart types
