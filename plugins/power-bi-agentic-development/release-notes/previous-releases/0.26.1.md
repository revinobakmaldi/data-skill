# v0.26.1

- Fixes and improvements to all hooks for windows users to account for issues with hook syntax in Claude Code on Windows. Fixes should ensure that 'hook error' notifications do not occur superfluously. Also added global kill switchces for hooks if you want to disable them (transiently or altogether) in the config.yaml for all /plugin/hooks/ dirs (for all plugins with hooks). Also documented the relevant issues in the README.
