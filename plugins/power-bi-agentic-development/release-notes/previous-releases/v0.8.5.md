# v0.8.5

- Major expansion of `pbi-desktop` plugin: DAX UDF documentation with full parameter type reference, visual query listener reference and subagent, Direct Lake partition details, model export via `te`/`fab` CLI, server timings, VertiPaq column statistics via DMVs
