# v0.8.7

- Fixed incorrect DAX in visual calculations (replaced invalid FILTER/ROWS/EARLIER patterns with RUNNINGSUM/MOVINGAVERAGE), corrected aggregation codes, and fixed datetime literal quoting across conditional formatting and expression references
- Updated schema versions throughout to match the bundled K201 example report, fixed legend position enumeration, and corrected TMDL syntax rules and file examples
- Clarified that `byConnection` definition.pbir now uses the simplified connectionString-only form; fixed wallpaper step to reference the correct file path
