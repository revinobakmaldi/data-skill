# v0.8.6

- Fix: `filterConfig` was incorrectly shown nested inside `visual {}` in `visual-json.md` top-level structure example; it belongs at the root container level alongside `$schema`, `name`, `position`, and `visual`
- Fix: `objects.section` (`verticalAlignment`) and `filterSortOrder` were undocumented in `report.md`; cross-referenced against actual PBIR example files
