# v0.8.8

- Fixed field reference docs (removed incorrect extension measure prefix, added required From array to slicer filter examples), corrected scatter chart query roles, and fixed aggregation code values across conditional formatting references
- Fixed rename cascade to include sortDefinition blocks (a commonly missed location), updated bookmarks docs to cover the expression field in filter snapshots, and corrected schema versions for page, visual container, and annotations
- Fixed several small inaccuracies: ImageURL→ImageUrl casing, isDefaultSort description, metadata selector description, and removed an internal contradiction in field discovery docs
