# v0.8.9

- Fixed TMDL depth table (the "depth 3" universal rule was wrong — body depth depends on declaration depth), expanded enumerations with confirmed tableEx visual types, markerShape, and lineStyle values
- Expanded rename cascade docs to cover bookmark highlight.dataMap keys and both Entity reference paths in bookmark filter snapshots; fixed report.json example with real schema versions and field meanings
- Removed a pbir CLI method from report-rebinding.md that referenced a command that doesn't exist
